
server:
  port: 8002

spring:
  application:
    name: service2
  cloud:
    config:
      profile: native
      uri: http://config-server-service/
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server-service/eureka/
  instance:
    hostname: http://service2-service/