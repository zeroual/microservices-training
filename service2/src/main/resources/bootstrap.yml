
server:
  port: 8002

spring:
  application:
    name: service2
  cloud:
    config:
      profile: native
      uri: http://localhost:9090/
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/