
server:
  port: 8001

spring:
  application:
    name: service1
  cloud:
    config:
      profile: native
      uri: http://localhost:9090/
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/
      leaseRenewalIntervalInSeconds: 10